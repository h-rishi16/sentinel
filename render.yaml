services:
  - type: web
    name: sentinel-risk-platform
    env: python
    plan: free

    region: oregon
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt && pip install -e ."
    startCommand: "./start.sh"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
